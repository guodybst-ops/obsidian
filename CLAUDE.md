# Obsidian 自动知识库上下文

请优先遵守根目录 `AGENTS.md` 中的工作规则。

这个 Vault 的目标是把零散资料沉淀成一个面向 AI 原生教育应用、教育内容产品和创业验证的知识库。用户偏好是：先理解真实业务目标，再把它落成目录、模板、提示词、规则和可执行清单。

处理 Obsidian 资料时，默认使用以下流程：

1. 新资料先进入 `01_收件箱/内容`。
2. 按相关性、新鲜度、价值度、可输出性、关联性做判断。
3. AI 教育相关知识、研究与项目统一放入 `02_AI教育`。
4. 其他执行事项放入 `03_其他项目`。
5. 原始资料和非 AI 教育知识放入 `04_资料库`。
6. 成品文章、报告、脚本、课程和周报放入 `05_内容输出`。
7. 复盘、提示词、规则、模板和附件放入 `99_系统与归档`。
8. 不再创建新的一级目录，日常从 `00_首页.md` 进入。

不要在未确认的情况下删除或批量移动已有资料。

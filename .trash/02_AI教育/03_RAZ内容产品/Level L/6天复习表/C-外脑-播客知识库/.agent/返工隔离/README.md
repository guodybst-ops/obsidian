# 返工隔离区

本目录用于保存从 `01人物原萃`、`02方法流程`、`03概念链接` 主动处理区移出的机械占位文件。

## 使用规则

1. 只隔离已经确认不符合当前写作规范、且不能通过原地返工变成合格成品的自动生成文件。
2. 隔离不是删除。原文件内容、文件名和来源信息必须保留，便于追溯旧处理过程。
3. 对应单集必须在专项审计日志和 `89单集笔记` 的 `## 拆解记录` 中写明：
   - 已产出合格文件；或
   - 已审读但不产出，并说明原因。
4. 本目录不计入 `01/02/03` 的有效完成数量。


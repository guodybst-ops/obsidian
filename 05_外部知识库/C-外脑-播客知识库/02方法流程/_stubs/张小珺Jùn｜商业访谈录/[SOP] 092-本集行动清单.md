---
tags: [方法, stub, 张小珺商业访谈录, 行动清单]
created: 2026-07-20
layer: extract
status: stub
provenance: external
viewpoint_owner: external_author
prefix_type: SOP
method_type: 行动清单
aliases: [092行动清单]
source_count: 1
revised_at: 2026-07-20
revised_reason: 改为直接基于 99 原始资料重写
sources:
  - show: "张小珺Jùn｜商业访谈录"
    episode: "092"
    speaker: "潘家怡"
---
# [SOP] 092-本集行动清单

## 一句话
把本集原始资料里最明确的做法信号落成一张执行清单。

## 什么时候用
- 你已经读过这一集的原始资料，准备把其中的做法转成自己的动作。
- 你想把嘉宾或叙事里的有效做法，改写成当前项目能执行的步骤。
- 你需要保留原始信号、落地动作、验证方式之间的对应关系。
- 红线：不适用于还没读原始资料、只想凭一句口号直接行动的场景。

## 输入
- 本集原始资料。
- 你当前要处理的一个具体问题、场景或决策。
- 一张可以记录动作、判断和验证结果的清单。

## 输出
- 4 条以内可执行动作。
- 每条动作对应的原始资料信号和验证方式。
- 完成信号：你已经把至少 1 条原始做法变成当前任务里的具体动作。

## 主流程

### Step 1: 执行第 1 条原始做法
- 原始资料信号：然后当然有可如果你用的是 PPO，而不是现在 kimi 跟 GRPO 用跟 DGRPO 用的这就是不带 value 方程，你就还需要训练一个 value model，所以你可能是有一个策略一个价值两个模型需要训练训练。
- 落地动作：把这句做法改写成你当前场景里的一个动作、判断条件或检查项。
- 完成信号：你已经写出谁来做、怎么验收。

### Step 2: 执行第 2 条原始做法
- 原始资料信号：然后同时的话像对于 R one zeroro 一下 R one 这两个模型的话，它主要的标志意义应该是说从 O one 发布之后，现到现在，大家终于知道就是用强化学习来可拓展的提高语言模型能力的整个技术路线的秘密。
- 落地动作：把这句做法改写成你当前场景里的一个动作、判断条件或检查项。
- 完成信号：你已经写出谁来做、怎么验收。

### Step 3: 执行第 3 条原始做法
- 原始资料信号：然后在这里的话，我觉得可以从一些背景开始讲起，这样子能让大家对整个就是为什么我们需要推理模型，以及它在就是整个技术眼睛上嗯的重要性有一些了解。
- 落地动作：把这句做法改写成你当前场景里的一个动作、判断条件或检查项。
- 完成信号：你已经写出谁来做、怎么验收。

### Step 4: 执行第 4 条原始做法
- 原始资料信号：然后再回答这个思维链其实一个比较简单的就是以及直观的类比，其实就是慢思考以及快思考模型可能会在这个思维链中先说哦，我要这么做，让我先把这个任务给它就是分成几个小任务，让我在一个个试，让我再试下这个题，它是不是这么做。
- 落地动作：把这句做法改写成你当前场景里的一个动作、判断条件或检查项。
- 完成信号：你已经写出谁来做、怎么验收。

## 判断点 / 决策树
- 如果原始资料给的是原则句：先把原则改写成动作，再执行。
- 如果原始资料给的是顺序句：按先后顺序拆成子任务。
- 如果原始资料给的是判断句：先补齐数据，再决定是否采用。
- 如果几条做法互相打架：先保留最贴近当前问题的那一条。

## 模板 / 清单 / 打分卡
| 原始资料信号 | 当前动作 | 验证方式 | 状态 |
|---|---|---|---|
| 然后当然有可如果你用的是 PPO，而不是现在 k… |  |  | [ ] |
| 然后同时的话像对于 R one zeroro 一… |  |  | [ ] |
| 然后在这里的话，我觉得可以从一些背景开始讲起，这… |  |  | [ ] |
| 然后再回答这个思维链其实一个比较简单的就是以及直… |  |  | [ ] |

## 常见坑
- 不要脱离原文上下文，只摘一句口号就开干。
- 不要把叙述句直接当流程，先翻成动作或判断条件。
- 不要同时追太多条线，优先执行最贴近当前问题的一条。
- 不要执行后不复盘，至少补一条验证结果。

## 附录：本集抽出的原始做法
- 1. 然后当然有可如果你用的是 PPO，而不是现在 kimi 跟 GRPO 用跟 DGRPO 用的这就是不带 value 方程，你就还需要训练一个 value model，所以你可能是有一个策略一个价值两个模型需要训练训练。
- 2. 然后同时的话像对于 R one zeroro 一下 R one 这两个模型的话，它主要的标志意义应该是说从 O one 发布之后，现到现在，大家终于知道就是用强化学习来可拓展的提高语言模型能力的整个技术路线的秘密。
- 3. 然后在这里的话，我觉得可以从一些背景开始讲起，这样子能让大家对整个就是为什么我们需要推理模型，以及它在就是整个技术眼睛上嗯的重要性有一些了解。
- 4. 然后再回答这个思维链其实一个比较简单的就是以及直观的类比，其实就是慢思考以及快思考模型可能会在这个思维链中先说哦，我要这么做，让我先把这个任务给它就是分成几个小任务，让我在一个个试，让我再试下这个题，它是不是这么做。

## 来源
- [[99原始资料/张小珺Jùn｜商业访谈录/092 - 89. 逐句讲解DeepSeek-R1、Kimi K1.5、OpenAI o1技术报告——“最优美的算法最干净”|原始资料]] — 仅从本集 99 原始资料抽取，不引用 89 单集笔记。
